@{
    Layout = "_Layout";
}

@using AnimalShelter.Models;

<h1>Animals<h1>
@if (Model.Count == 0)
{
    <h3>No animals have been added yet!</h3>
}

@foreach (Animal animal in Model)
{
    <!-- <li>@animal.Breed </li>
    <li>@animal.Name</li>
    <li>@animal.Gender</li>
    <li>@animal.Date</li> -->
    <li>@Html.ActionLink($"{animal.Breed }", "Details", new { id = animal.Id })</li>
}

<p>@Html.ActionLink("Add new animal", "Create")</p>